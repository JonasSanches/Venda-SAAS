"use client";import{useEffect}from"react";export function TenantBranding(){useEffect(()=>{const apply=()=>{try{const s=JSON.parse(localStorage.getItem("varejo-session")??"{}");if(!s.tenant?.logoDataUrl)return;document.querySelectorAll<HTMLElement>("main aside .brand").forEach(el=>{if(el.querySelector(".tenant-logo"))return;el.innerHTML="";const img=document.createElement("img");img.src=s.tenant.logoDataUrl;img.alt=s.tenant.name;img.className="tenant-logo";el.append(img)})}catch{}};apply();const observer=new MutationObserver(apply);observer.observe(document.body,{childList:true,subtree:true});return()=>observer.disconnect()},[]);return null}
