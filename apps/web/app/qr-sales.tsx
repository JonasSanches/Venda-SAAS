"use client";import { FormEvent,useEffect,useRef,useState } from "react";import { request } from "./api-client";
export function QrSales({token}:{token:string}){const[links,setLinks]=useState<any[]>([]),[message,setMessage]=useState(""),[error,setError]=useState(""),[selected,setSelected]=useState<any>();const canvas=useRef<HTMLCanvasElement>(null);const load=()=>request<any[]>("/qr-checkout/links",token).then(setLinks).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);useEffect(()=>{if(selected&&canvas.current){void import("qrcode").then(QR=>QR.toCanvas(canvas.current!,`${location.origin}/comprar/${selected.token}`,{width:260,margin:2}))}},[selected]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const result=await request<any>("/qr-checkout/links",token,{method:"POST",body:JSON.stringify({name:f.get("name"),deliveryEnabled:f.get("deliveryEnabled")==="on",addressRequired:f.get("addressRequired")==="on",offers:[{title:f.get("title"),description:f.get("description"),price:Number(f.get("price"))}]})});setSelected(result);setMessage("QR de venda criado. Você já pode imprimir ou compartilhar.");e.currentTarget.reset();await load()}catch(err){setError((err as Error).message)}}return <section className="qr-sales"><header><div><small>VENDA POR QR CODE</small><h2>Receba pedidos sem atendente</h2><p>Crie uma página pública, imprima o QR e receba o pagamento pelo Mercado Pago.</p></div></header>{message&&<div className="success">{message}</div>}{error&&<div className="error">{error}</div>}<form className="email-form" onSubmit={create}><h3>Novo QR de venda</h3><input name="name" placeholder="Nome do QR (ex.: Balcão da loja)" required/><input name="title" placeholder="Produto ou serviço" required/><input name="price" type="number" min="0.01" step="0.01" placeholder="Preço" required/><input name="description" placeholder="Descrição (opcional)"/><label><input name="deliveryEnabled" type="checkbox"/> Oferecer entrega</label><label><input name="addressRequired" type="checkbox"/> Exigir endereço</label><button>Criar QR Code</button></form>{selected&&<section className="qr-created"><canvas ref={canvas}/><div><h3>{selected.name}</h3><p>Imprima este QR Code e deixe-o no local de venda.</p><a href={selected.url} target="_blank" rel="noreferrer">Abrir página de compra</a></div></section>}<section className="email-list"><h3>QR Codes criados</h3>{links.map(link=><article className="qr-link" key={link.id}><div><strong>{link.name}</strong><span>{link.offers.length} oferta(s) · {link._count?.orders??0} pedido(s)</span></div><button className="secondary" onClick={()=>setSelected({...link,url:`${location.origin}/comprar/${link.token}`})}>Ver QR</button></article>)}</section></section>}
