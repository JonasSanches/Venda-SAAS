"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3101/api";
export default function Comprar({params}:{params:Promise<{token:string}>}){const[token,setToken]=useState(""),[shop,setShop]=useState<any>(),[cart,setCart]=useState<Record<string,number>>({}),[message,setMessage]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);useEffect(()=>{void params.then(({token})=>{setToken(token);fetch(`${API}/qr-checkout/${token}`).then(async r=>{const data=await r.json();if(!r.ok)throw Error(data.message??"QR indisponível");return data}).then(setShop).catch(e=>setError(e.message))})},[params]);const total=useMemo(()=>shop?.offers.reduce((sum:number,o:any)=>sum+(cart[o.id]??0)*o.price,0)??0,[shop,cart]);async function checkout(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!Object.values(cart).some(Boolean)){setError("Escolha pelo menos um item.");return}setLoading(true);setError("");const f=new FormData(e.currentTarget);try{const r=await fetch(`${API}/qr-checkout/${token}/checkout`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({buyerName:f.get("buyerName"),buyerEmail:f.get("buyerEmail")||undefined,buyerPhone:f.get("buyerPhone")||undefined,deliveryAddress:f.get("deliveryAddress")||undefined,items:Object.entries(cart).filter(([,q])=>q>0).map(([offerId,quantity])=>({offerId,quantity}))})}),data=await r.json();if(!r.ok)throw Error(data.message??"Pagamento indisponível");location.href=data.checkoutUrl}catch(e){setError((e as Error).message);setLoading(false)}}if(error&&!shop)return <main className="qr-public"><p>{error}</p></main>;return <main className="qr-public"><section>{message&&<div className="success">{message}</div>}<small>VENDA ONLINE</small><h1>{shop?.company?.name??"Carregando..."}</h1><h2>{shop?.name}</h2><p>Escolha os itens e pague com Pix, cartão ou outro meio disponível no Mercado Pago.</p><div className="qr-offers">{shop?.offers.map((offer:any)=><article key={offer.id}><div><h3>{offer.title}</h3><p>{offer.description}</p><b>{offer.price.toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</b></div><div className="qr-quantity"><button onClick={()=>setCart(c=>({...c,[offer.id]:Math.max(0,(c[offer.id]??0)-1)}))}>−</button><span>{cart[offer.id]??0}</span><button onClick={()=>setCart(c=>({...c,[offer.id]:(c[offer.id]??0)+1}))}>+</button></div></article>)}</div><form onSubmit={checkout}><h3>Seus dados</h3><label>Nome<input name="buyerName" required/></label><label>E-mail (opcional)<input name="buyerEmail" type="email"/></label><label>Telefone (opcional)<input name="buyerPhone"/></label>{shop?.deliveryEnabled&&<label>Endereço para entrega{shop.addressRequired?" *":" (opcional)"}<textarea name="deliveryAddress" required={shop.addressRequired}/></label>}<strong>Total: {total.toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</strong><button disabled={loading}>{loading?"Abrindo pagamento...":"Pagar com Mercado Pago"}</button></form>{error&&<div className="error">{error}</div>}</section></main>}
