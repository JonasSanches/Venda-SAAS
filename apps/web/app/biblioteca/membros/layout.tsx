import type { Metadata } from "next";

export const metadata:Metadata={title:"Área de membros | Biblioteca Venda+",description:"Acesso reservado para membros da Biblioteca Digital Venda+."};

export default function BibliotecaMembrosLayout({children}:{children:React.ReactNode}){return children}
