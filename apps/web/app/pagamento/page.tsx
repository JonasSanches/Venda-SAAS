"use client";
import{FormEvent,useEffect,useState}from"react";
const API=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3101/api";
type Info={name:string;status:string;amount:number;methods:string[]};
export default function Payment(){const[tenantId,setTenantId]=useState(""),[info,setInfo]=useState<Info|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(false),[result,setResult]=useState("");useEffect(()=>{const params=new URLSearchParams(location.search),id=params.get("cliente")??"";setTenantId(id);setResult(params.get("resultado")??"");if(id)fetch(`${API}/billing/public/${id}`).then(async r=>{const body=await r.json();if(!r.ok)throw Error(body.message??"Cobrança não encontrada");return body}).then(setInfo).catch(e=>setError(e.message));else setError("Link de pagamento inválido")},[]);async function checkout(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);try{const r=await fetch(`${API}/billing/public/${tenantId}/checkout`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email")})}),body=await r.json();if(!r.ok)throw Error(Array.isArray(body.message)?body.message.join(", "):body.message??"Pagamento indisponível");location.href=body.checkoutUrl}catch(e){setError((e as Error).message);setLoading(false)}}return <main className="payment-page"><section><div className="brand dark"><span>V</span> Venda Mais <a className="omega-credit" href="https://omega-ia.com" target="_blank" rel="noopener noreferrer">by Omega</a></div>{result&&<div className={result==="sucesso"?"success":"notice"}>{result==="sucesso"?"Pagamento recebido. A confirmação será processada automaticamente.":result==="pendente"?"Pagamento pendente. PIX e boleto podem levar algum tempo para confirmação.":"O pagamento não foi concluído. Você pode tentar novamente."}</div>}<small>ASSINATURA VENDA MAIS</small><h1>{info?.name??"Carregando cobrança..."}</h1>{info&&<><div className="payment-price"><span>Plano mensal</span><strong>{info.amount.toLocaleString("pt-BR",{style:"currency",currency:"BRL"})}</strong></div><p>Escolha a forma de pagamento no ambiente seguro do Mercado Pago.</p><div className="payment-methods">{info.methods.map(method=><span key={method}>{method}</span>)}</div><form onSubmit={checkout}><label>E-mail do pagador<input name="email" type="email" required/></label><button disabled={loading}>{loading?"Abrindo Mercado Pago...":"Continuar para pagamento"}</button></form></>}{error&&<div className="error">{error}</div>}<a href="/">Voltar ao Venda Mais</a></section></main>}
