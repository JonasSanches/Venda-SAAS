import { BadGatewayException, BadRequestException, Injectable, NotFoundException } from "@nestjs/common";import { prisma,withTenant } from "@varejo/database";import { randomBytes,randomUUID } from "node:crypto";
@Injectable()export class QrCheckoutService{private token(){const value=process.env.MERCADO_PAGO_ACCESS_TOKEN;if(!value)throw new BadRequestException("Mercado Pago não configurado.");return value}private base(){return(process.env.PUBLIC_APP_URL??"https://vendamais-app.com").replace(/\/$/,"")}async links(tenantId:string){return withTenant(tenantId,tx=>tx.qrCheckoutLink.findMany({where:{tenantId},include:{offers:true,_count:{select:{orders:true}}},orderBy:{createdAt:"desc"}}))}async create(tenantId:string,input:any){const value=await withTenant(tenantId,tx=>tx.qrCheckoutLink.create({data:{tenantId,name:input.name.trim(),token:randomBytes(24).toString("base64url"),deliveryEnabled:input.deliveryEnabled,addressRequired:input.deliveryEnabled&&input.addressRequired,offers:{create:input.offers.map((o:any)=>({productId:o.productId??null,title:o.title.trim(),description:o.description?.trim()||null,price:o.price}))}},include:{offers:true}}));return{...value,url:`${this.base()}/comprar/${value.token}`}}async remove(tenantId:string,id:string){const item=await withTenant(tenantId,tx=>tx.qrCheckoutLink.findFirst({where:{id,tenantId}}));if(!item)throw new NotFoundException("QR de venda não encontrado.");await withTenant(tenantId,tx=>tx.qrCheckoutLink.update({where:{id},data:{active:false}}));return{ok:true}}async publicLink(token:string){const item=await prisma.qrCheckoutLink.findFirst({where:{token,active:true},include:{tenant:{select:{name:true,logoDataUrl:true,phone:true}},offers:{where:{active:true},orderBy:{createdAt:"asc"}}}});if(!item)throw new NotFoundException("Este QR Code não está disponível.");return{id:item.id,name:item.name,deliveryEnabled:item.deliveryEnabled,addressRequired:item.addressRequired,company:item.tenant,offers:item.offers.map(o=>({id:o.id,title:o.title,description:o.description,price:Number(o.price)}))}}async checkout(token:string,input:any){const link=await prisma.qrCheckoutLink.findFirst({where:{token,active:true},include:{offers:{where:{active:true}}}});if(!link)throw new NotFoundException("QR Code não encontrado.");if(link.addressRequired&&!input.deliveryAddress?.trim())throw new BadRequestException("Informe o endereço para entrega.");const selected=new Map(input.items.map((i:any)=>[i.offerId,i.quantity]));const items=link.offers.filter(o=>selected.has(o.id)).map(o=>({offer:o,quantity:Number(selected.get(o.id))}));if(!items.length||items.some(i=>!Number.isInteger(i.quantity)||i.quantity<1))throw new BadRequestException("Selecione itens válidos.");const total=Number(items.reduce((sum,i)=>sum+Number(i.offer.price)*i.quantity,0).toFixed(2)),externalReference=randomUUID(),order=await prisma.qrCheckoutOrder.create({data:{tenantId:link.tenantId,linkId:link.id,externalReference,buyerName:input.buyerName.trim(),buyerEmail:input.buyerEmail?.trim()||null,buyerPhone:input.buyerPhone?.trim()||null,deliveryAddress:link.deliveryEnabled?input.deliveryAddress?.trim()||null:null,total,items:{create:items.map(i=>({offerId:i.offer.id,productId:i.offer.productId,title:i.offer.title,quantity:i.quantity,unitPrice:i.offer.price,total:Number(i.offer.price)*i.quantity}))}}});const callback=`${this.base()}/comprar/${token}?pedido=${order.id}`,response=await fetch("https://api.mercadopago.com/checkout/preferences",{method:"POST",headers:{Authorization:`Bearer ${this.token()}`,"Content-Type":"application/json","X-Idempotency-Key":externalReference},body:JSON.stringify({items:items.map(i=>({id:i.offer.id,title:i.offer.title,description:i.offer.description??undefined,quantity:i.quantity,currency_id:"BRL",unit_price:Number(i.offer.price)})),payer:input.buyerEmail?{email:input.buyerEmail}:undefined,external_reference:externalReference,notification_url:`${this.base()}/api/qr-checkout/webhook`,back_urls:{success:`${callback}&resultado=sucesso`,pending:`${callback}&resultado=pendente`,failure:`${callback}&resultado=falha`},auto_return:"approved",statement_descriptor:"VENDAMAIS"})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new BadGatewayException(data.message??"Não foi possível iniciar o pagamento.");return{checkoutUrl:process.env.MERCADO_PAGO_SANDBOX==="true"?data.sandbox_init_point:data.init_point}}async webhook(paymentId:string){if(!paymentId)return{received:true};const response=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(paymentId)}`,{headers:{Authorization:`Bearer ${this.token()}`}});if(!response.ok)throw new BadGatewayException("Não foi possível validar o pagamento.");const data:any=await response.json(),order=await prisma.qrCheckoutOrder.findUnique({where:{externalReference:String(data.external_reference??"")}});if(!order)return{received:true};if(Number(data.transaction_amount)!==Number(order.total))throw new BadRequestException("Valor não confere.");const approved=data.status==="approved";await prisma.qrCheckoutOrder.update({where:{id:order.id},data:{providerPaymentId:String(data.id),status:approved?"APPROVED":data.status==="rejected"?"REJECTED":"PENDING",paidAt:approved?new Date(data.date_approved??Date.now()):null}});return{received:true}}}
